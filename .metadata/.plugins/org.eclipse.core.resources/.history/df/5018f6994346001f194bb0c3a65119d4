package com.gamestore.services;

import com.gamestore.domain.Contact;

public interface ContactService {
	Contact addNewContactInfo(Long id, String username , String email , String msg);
	Contact save(Contact contact);
}
